defaults:
  - wan_i2v
  - _self_

text_encoder:
  ckpt_path: null

vae:
  ckpt_path: null
 
clip:
  ckpt_path: null

model:
  ckpt_path: null
  tuned_ckpt_path: null # Because wan_toy.yaml inherits from wan_i2v.yaml, we need to override the parent class's tuned_ckpt_path to null
  dim: 128
  ffn_dim: 128
  num_heads: 4
  num_layers: 2
